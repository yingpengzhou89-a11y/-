# Project Custom Rules

## Recruitment Task
- Do not perform any single recruitments (either free single summon or tickets summon).
- The advanced recruitment task must only perform 10x recruitments and then safely return.
- Keep the advanced 10x recruitment coordinates locked to `{"x": 800, "y": 650}` (calibrated by the user). Do not restore it to `766` or any other values.
